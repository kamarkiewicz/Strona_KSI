export const state = () => ({
})

export const mutations = {
}

export const actions = {
  // nuxtServerInit ({ dispatch }, { app }) {
  //   dispatch('album/getEntries', app)
  // }
}

export const getters = {
}
